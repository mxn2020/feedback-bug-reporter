/**
 * App configuration — customize these values for your app.
 */
export const APP_CONFIG = {
  slug: "feedback-bug-reporter",
  title: "Feedback & Bug Reporter",
  description:
    "Embeddable feedback widget and bug reporting system.",
} as const;
